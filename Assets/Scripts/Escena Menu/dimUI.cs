using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System.IO;

public class dimUI : MonoBehaviour
{
    float x, y;

    // Start is called before the first frame update
    void Awake()
    {
        //x = (Screen.width * transform.position.x) / 1920.0f;
        //y= (Screen.height * transform.position.y) / 900.0f;
        //transform.position = new Vector3(x, y, transform.position.z);
        //x = (Screen.width * transform.localScale.x) / 1920.0f;
        //y = (Screen.height * transform.localScale.y) / 900.0f;
        //transform.localScale = new Vector3(x, y, transform.localScale.z);
    }

}
